# CLAUDE.md

Follow the repository rules in `AGENTS.md`.

Also read `docs/SPEC.md`, `docs/TEST_PLAN.md`, and `docs/AI_VALIDATION.md` before non-trivial implementation or when context is uncertain.

Do not duplicate or override repository policy here unless the user explicitly requests a Claude-specific exception.
