# Automation & Supervision

## Multica supervisor

Autopilot:
- Title: `AIME GitHub Supervisor`
- Agent: `Oracle Codex`
- Mode: `run_only`
- Project: `AI Test`
- Trigger: generic webhook

The supervisor reviews GitHub push / pull_request events and then:
- reads `AGENTS.md` and canonical docs
- checks open PRs and active Multica issues
- detects build/test/type/API-contract/spec/conflict/security problems
- comments on the existing responsible issue where possible
- creates a new issue only for distinct cross-cutting work
- routes fixes to the smallest suitable agent
- reviews PR readiness
- merges only when checks pass, conflicts are resolved, and no security/spec blockers remain
- avoids noisy duplicate comments/issues

## GitHub Actions workflow

Workflow:
`.github/workflows/multica-supervisor.yml`

Triggers:
- push to `main`
- push to `feature/**`
- pull request opened/synchronized/reopened/ready-for-review/closed
- manual `workflow_dispatch`

## Required GitHub secret

Create this repository Actions secret:

```text
MULTICA_AIME_SUPERVISOR_WEBHOOK_URL
```

Its value is the private webhook URL generated by the Multica autopilot trigger.

Do not commit the webhook URL anywhere in the repository, issues, PR comments, logs, screenshots, or frontend assets.

## Security

The webhook URL is a credential. Rotate it if exposed.

The workflow sends only minimal GitHub metadata needed for supervision:
- event/action
- repository
- ref/SHA
- actor
- Actions run URL
- PR number/URL/state/branches

It does not send repository secrets or file contents.
