# AIME Decision Review — unified runtime configuration
# Names and purposes only. Keep real values in a local, git-ignored .env file
# or in the deployment secret store. Never pass backend secrets to web.

# Frontend build/runtime
WEB_PORT=
VITE_API_BASE_URL=

# Backend runtime (Compose maps API_PORT to the API process PORT)
API_PORT=
SQLITE_PATH=
LOG_LEVEL=

# LLM provider (server-only)
LLM_PROVIDER=
LLM_MODEL=
LLM_BASE_URL=
LLM_API_KEY=

# Fuyao / Hithink MCP (server-only)
HITHINK_FINANCE_BASE_URL=
HITHINK_FINANCE_API_KEY=
HITHINK_FINANCE_SERVERS=

# iFinD MCP (server-only)
IFIND_MCP_BASE_URL=
IFIND_MCP_AUTHORIZATION=
IFIND_MCP_SERVERS=
