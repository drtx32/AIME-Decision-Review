{
  "name": "aime-decision-review",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down",
    "preflight": "node scripts/preflight.mjs",
    "preflight:zip": "node scripts/preflight.mjs --zip dist/submission.zip",
    "preflight:strict": "node scripts/preflight.mjs --strict",
    "preflight:test": "cd apps/api && bun test ../../tests/preflight/",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "echarts": "^5.6.0",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "^5.0.1"
  }
}
