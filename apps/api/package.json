{
  "name": "@aime/decision-review-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIME Decision Review API — Bun + Hono + OpenAI Agents SDK TS backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openai/agents": "^0.1.9",
    "hono": "^4.6.14",
    "openai": "^5.23.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}