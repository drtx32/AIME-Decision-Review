# AIME Decision Review — unified runtime configuration
# Copy to .env for local/host deployment. Keep real values in the local,
# git-ignored .env file or a deployment secret store. Never pass backend
# secrets to the web build.

# Compose identity / frontend entrypoint
# Keep this project name stable so moving the working directory does not create
# a fresh SQLite volume under a different Compose project name.
COMPOSE_PROJECT_NAME=aime-decision-review
WEB_PORT=13608
VITE_API_BASE_URL=/api

# Backend runtime (API is Compose-internal on port 3000; no host API port)
SQLITE_PATH=
LOG_LEVEL=

# Bootstrap admin (server-only, required on first startup). On a fresh
# database the API creates this account and forces a password change at
# first login. The username is a public label and defaults to "admin"
# when unset; the password has no default — it MUST be supplied via the
# host environment, .env file, or deployment secret store. The API
# refuses to start on a fresh DB without this value, so a misconfigured
# deployment fails fast instead of running with a public credential.
# Never commit a real password.
INITIAL_ADMIN_USERNAME=
INITIAL_ADMIN_PASSWORD=

# LLM provider (server-only)
LLM_PROVIDER=
LLM_MODEL=
LLM_BASE_URL=
LLM_API_KEY=

# Fuyao / Hithink MCP (server-only)
HITHINK_FINANCE_BASE_URL=
HITHINK_FINANCE_API_KEY=
HITHINK_FINANCE_SERVERS=
# Optional intent:tool mapping for live MCP; leave empty until verified by the operator.
HITHINK_FINANCE_TOOL_MAP=
# Optional server:intent:toolName map; when set, only listed servers claim tools.
# HITHINK_FINANCE_TOOL_MAP_PER_SERVER=a-share:price:get_a_share_prices_snapshot
HITHINK_FINANCE_TOOL_MAP_PER_SERVER=
HITHINK_FINANCE_REMOTE_SUFFIX_MAP=

# iFinD MCP (server-only)
IFIND_MCP_BASE_URL=
IFIND_MCP_AUTHORIZATION=
IFIND_MCP_SERVERS=
IFIND_MCP_TOOL_MAP=
# Optional server:intent:toolName map for iFinD.
# IFIND_MCP_TOOL_MAP_PER_SERVER=stock:news:search_news,news:news:search_news
IFIND_MCP_TOOL_MAP_PER_SERVER=
IFIND_MCP_REMOTE_SUFFIX_MAP=
