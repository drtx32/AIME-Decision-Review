# AIME Decision Review API — environment variable names only.
# Copy to .env (which is git-ignored) and fill in real values locally.
# Never commit real keys/tokens/Authorization headers to the repo.

# ── LLM provider (thin abstraction; v0.1 implements one provider only) ─────
# Provider identifier — kept for future extension; current runtime only reads
# LLM_BASE_URL / LLM_MODEL / LLM_API_KEY. Allowed values: openai-compatible,
# mock.
LLM_PROVIDER=mock
# OpenAI-compatible model id, e.g. gpt-4o-mini, MiniMax-Text-01, etc.
LLM_MODEL=mvp-mock-model
# Base URL for the OpenAI-compatible chat completions endpoint.
# Leave empty when LLM_PROVIDER=mock to run the vertical slice without secrets.
LLM_BASE_URL=
# API key for the LLM provider. Loaded only from server env / GitHub Secrets.
LLM_API_KEY=

# ── Fuyao (Hithink) finance MCP — configure all six, load by intent ─────────
# Endpoint base for the Fuyao MCP gateway. Empty disables the adapter.
HITHINK_FINANCE_BASE_URL=
HITHINK_FINANCE_API_KEY=
# Comma-separated servers to register in the registry. Allowed values:
# meta,a-share,a-share-index,fund,futures,options
HITHINK_FINANCE_SERVERS=meta,a-share

# ── iFinD MCP — configure all eleven, load by intent ────────────────────────
IFIND_MCP_BASE_URL=
IFIND_MCP_AUTHORIZATION=
# Comma-separated servers to register in the registry. Allowed values:
# ds,enterprise,law,stock,fund,edb,news,bond,global-stock,index,futures
IFIND_MCP_SERVERS=stock,news,index

# ── Runtime ────────────────────────────────────────────────────────────────
PORT=3000
SQLITE_PATH=./data/decision-review.db
LOG_LEVEL=info